[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "djohodo-demo"
version = "0.1.0"
description = "Demo Streamlit pour Djohodo"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
